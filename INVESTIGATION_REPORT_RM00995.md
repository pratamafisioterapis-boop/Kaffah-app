# HASIL INVESTIGASI RM00995

## 1. Verifikasi Kode (Code Audit)
Kami telah memeriksa seluruh alur data dari Database -> API -> UI Component.

### A. API Layer (`src/lib/api.js`)
Function `getPatients()` menggunakan query: